version: "3.9"

services:
  api:
    build: ./services/api
    ports:
      - "8000:8000"
    environment:
      - DEBUG=true
    volumes:
      - ./services/api/app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
