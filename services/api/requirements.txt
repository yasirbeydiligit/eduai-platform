# Production bağımlılıkları — bu dosya Dockerfile tarafından kullanılır.
# Dev/test araçları requirements-dev.txt'te (oradaki `-r requirements.txt` bu dosyayı da çeker).
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.2.0
structlog>=24.1.0
python-multipart>=0.0.9
